server:
  port: 5058

spring:
  application:
    name: mulemind-document-generation-service

  cloud:
    openfeign:
      client:
        config:
          default:
            connectTimeout: 10000      # 10 seconds
            readTimeout: 300000        # 5 minutes
            loggerLevel: full

  kafka:
    bootstrap-servers: localhost:9092
    producer:
      key-serializer: org.apache.kafka.common.serialization.StringSerializer
      value-serializer: org.springframework.kafka.support.serializer.JsonSerializer
    consumer:
      group-id: ${KAFKA_CONSUMER_GROUP_ID:mulemind-document-service-group-dev}
      auto-offset-reset: latest
      enable-auto-commit: true
      key-deserializer: org.springframework.kafka.support.serializer.ErrorHandlingDeserializer
      value-deserializer: org.springframework.kafka.support.serializer.ErrorHandlingDeserializer
      properties:
        spring.deserializer.key.delegate.class: org.apache.kafka.common.serialization.StringDeserializer
        spring.deserializer.value.delegate.class: org.springframework.kafka.support.serializer.JsonDeserializer
        spring.json.trusted.packages: "*"
        spring.json.use.type.headers: false
        spring.json.value.default.type: com.mulemind.document.dto.MetadataGeneratedEvent

  datasource:
    url: jdbc:mysql://localhost:3306/mulemind_db
    username: root
    password: ellenverma

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

app:
  scan-event:
    type: MULE_APPLICATION_DOCUMENT_GENERATION
    version: "1.0"
  kafka:
    topic:
      mulemind-metadata-generated-event: mules-metadata-generated


document:
  service:
    url: http://localhost:5054

ai:
  service:
    url: http://localhost:5056

minio:
  endpoint: http://localhost:9000
  access-key: minioadmin
  secret-key: minioadmin
  bucket-name: documents

project:
  archive:
    storage-path: /Users/amreshverma/mule_down_extract
    extraction-folder: /Users/amreshverma/mule_down_extract
    temp-directory-prefix: mulemind-project-